/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DImageFallbackDirective } from './DImageFallback.directive';

describe('Directive: DImageFallback', () => {
  it('should create an instance', () => {
    const directive = new DImageFallbackDirective();
    expect(directive).toBeTruthy();
  });
});
